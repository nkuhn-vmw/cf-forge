applications:
  - name: cf-forge-api
    memory: 1G
    instances: 1
    buildpack: java_buildpack_offline
    path: target/cf-forge-api-0.1.0-SNAPSHOT.jar
    health-check-type: http
    health-check-http-endpoint: /actuator/health
    log-rate-limit-per-second: -1
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 21.+ } }'
      SPRING_PROFILES_ACTIVE: cloud
    services:
      - cf-forge-db
      - cf-forge-cache
      - cf-forge-mq
      - cf-forge-sso
    routes:
      - route: cf-forge-api.apps.tas-ndc.kuhn-labs.com
